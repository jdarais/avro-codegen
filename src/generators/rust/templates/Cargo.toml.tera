[package]
name = "{{ package.name }}"
version = "{{ package.version }}"
description = "{{ package.description }}"
edition = "2021"
rust-version = "1.81"

[dependencies]
apache-avro = "0.19.0"
serde = "1.0.228"
